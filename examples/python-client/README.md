## Example client in python

This sends a request to the web server, if it is running, containing a specification
and the `project.json` file from `sum-from-to-with-repeat-loop.sb3` from the `sb3` folder.

Run with `uv run main.py`

Currently, the server will only print the request to stdin without sending an interesting response.
